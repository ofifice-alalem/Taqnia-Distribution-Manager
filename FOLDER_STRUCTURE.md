# 📂 هيكل المجلدات - صفحة المحلات

## Backend Structure

```
backend/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       └── Admin/
│   │           └── Stores/              ← مجلد خاص بإدارة المحلات
│   │               └── StoreController.php
│   │
│   └── Models/
│       └── Store/
│           └── Store.php                ← تم تحديثه بإضافة علاقة debtLedger
│
├── resources/
│   └── views/
│       └── admin/
│           └── stores/                  ← مجلد خاص بواجهات المحلات
│               └── index.blade.php
│
└── routes/
    └── web.php                          ← تم إضافة route للمحلات
```

## 🎯 المبدأ المتبع

✅ **كل عملية في مجلد منفصل**
- Controllers منظمة حسب الدور والعملية
- Views منظمة بنفس الهيكل
- سهولة الصيانة والتوسع

## 📝 مثال على التنظيم

```
Admin/
├── Promotions/          ← عروض التخفيض
│   └── PromotionController.php
│
├── Stores/              ← المحلات
│   └── StoreController.php
│
└── Users/               ← (مستقبلاً) إدارة المستخدمين
    └── UserController.php
```

## 🚀 الاستخدام

**الوصول للصفحة:**
```
URL: /admin/stores
Route Name: admin.stores.index
```

**في Blade:**
```blade
<a href="{{ route('admin.stores.index') }}">المتاجر</a>
```

**في Controller:**
```php
return redirect()->route('admin.stores.index');
```
