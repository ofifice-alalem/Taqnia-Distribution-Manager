# صفحة إدارة المحلات - Admin

## 📁 الملفات المنشأة

### 1. Controller
**المسار:** `backend/app/Http/Controllers/Admin/Stores/StoreController.php`

**الوظيفة:**
- عرض قائمة المحلات
- حساب إجمالي المدفوعات لكل محل
- حساب إجمالي الديون (المبيعات - الإرجاعات)
- حساب المتبقي (الدين - المدفوع)

**الاستعلام:**
```php
- total_paid: مجموع المدفوعات (entry_type = 'payment')
- total_debt: مجموع المبيعات (entry_type = 'sale')
- total_returns: مجموع الإرجاعات (entry_type = 'return')
- remaining: الدين الصافي - المدفوع
```

---

### 2. View
**المسار:** `backend/resources/views/admin/stores/index.blade.php`

**المحتوى:**
- جدول يعرض جميع المحلات
- الأعمدة:
  - # (الترقيم)
  - اسم المحل (مع اسم المالك)
  - الهاتف
  - ما تم دفعه (badge أخضر)
  - الدين (badge أصفر)
  - المتبقي (badge أحمر/أزرق/رمادي حسب الحالة)
  - الإجراءات (عرض/تعديل/سجل الديون)

---

### 3. Route
**المسار:** `backend/routes/web.php`

**الإضافة:**
```php
Route::get('/stores', [StoreController::class, 'index'])->name('admin.stores.index');
```

**الحماية:** middleware `auth` و `role:admin`

---

### 4. تحديث القائمة الجانبية
**المسار:** `backend/resources/views/layouts/app.blade.php`

تم تفعيل رابط "المتاجر" في قسم الإدارة للـ Admin

---

## 🎯 كيفية الوصول

1. تسجيل الدخول كـ **Admin**
2. من القائمة الجانبية → **الإدارة** → **المتاجر**
3. أو مباشرة: `/admin/stores`

---

## 📊 منطق الحسابات

### الدين الصافي
```
الدين = (إجمالي المبيعات - إجمالي الإرجاعات)
```

### المتبقي
```
المتبقي = الدين الصافي - إجمالي المدفوعات
```

### حالات المتبقي
- **موجب (أحمر):** المحل مدين
- **سالب (أزرق):** المحل له رصيد زائد
- **صفر (رمادي):** لا يوجد دين أو رصيد

---

## 🔄 الخطوات التالية (اختياري)

يمكن إضافة:
1. صفحة تفاصيل المحل
2. صفحة تعديل بيانات المحل
3. صفحة سجل الديون التفصيلي
4. صفحة إضافة محل جديد
5. تصدير البيانات (Excel/PDF)
6. فلترة وبحث في الجدول

---

## ✅ الحالة
- [x] Controller منظم في مجلد `Admin/Stores`
- [x] View منظم في مجلد `admin/stores`
- [x] Route مضاف ومحمي
- [x] القائمة الجانبية محدثة
- [x] الحسابات دقيقة حسب النظام
